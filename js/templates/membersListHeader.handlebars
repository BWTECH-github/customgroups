<div>
	<div class="group-name-title">
		{{!-- Platzhaltergrafik zum Gruppennamen, rein schmückend --}}
		<span class="avatar" aria-hidden="true"></span>
		<span class="group-name-title-display">{{groupName}}</span>
	</div>

	{{!-- der Tooltip entfernt title aus dem Markup, deshalb der unsichtbare
	      Name im Knopf --}}
	<a class="close action-close icon-close" role="button" href="#" title="{{closeLabel}}"><span class="hidden-visually">{{closeLabel}}</span></a>
</div>
<div class="custom-group-buttons">
	{{#if userIsMember}}
		<input class="action-leave-group" type="button" value="{{leaveGroupLabel}}" />
	{{/if}}
	{{#if canAdmin}}
		<form class="custom-group-import-form" enctype="multipart/form-data" method="post" name="custom-group-csv-import">
			<a class="button action-export-csv" href="{{downloadUrl}}">{{exportCsvLabel}}</a>
			{{!-- Das Dateifeld ist ausgeblendet; ohne tabindex war der Import per
			      Tastatur nicht erreichbar. Enter und Leertaste öffnen die
			      Dateiauswahl (MembersView._onImportKeyDown). --}}
			<label for="custom-group-import-elem" class="custom-group-import-label button" role="button" tabindex="0">{{importCsvLabel}}</label>
			<input id="custom-group-import-elem" name="csv-input" type="file" accept="text/csv" />
		</form>
	{{/if}}
</div>
{{#if canAdmin}}
	<div class="add-member-container"></div>
{{/if}}
